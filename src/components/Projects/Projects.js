import React from 'react';
import './Projects.css';

const Projects = ({ id }) => {
  return (
    <div id={id} className="content-projects-wrapper">
      <h1>Projects</h1>
    </div>
  );
};

export default Projects;
